# Solar Router Ollama Prompt

Before doing any work, read the applicable `AGENTS.md` files for the current scope.

Minimum order:
- Read root `AGENTS.md` first.
- If working in `core/`, read `core/AGENTS.md`.
- If working in `planets/<planet-name>/`, read `planets/<planet-name>/AGENTS.md`.

Follow the most specific governance file that applies to the task.
