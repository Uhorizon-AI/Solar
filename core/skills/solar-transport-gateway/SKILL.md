---
name: solar-transport-gateway
description: >
  Deprecated alias — renamed to solar-gateway (2026-05-31). Sync maps this stub one release;
  invoke skill solar-gateway for HTTP webhooks and WebSocket ingress.
---

# Renamed: solar-gateway

This skill was renamed to **`solar-gateway`**.

- Use `core/skills/solar-gateway/` and invoke skill `solar-gateway`.
- Production webhooks (n8n, Telegram) are unchanged; only the skill folder name changed.
